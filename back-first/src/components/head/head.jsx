import React,{Component} from "react"
import { Layout} from 'antd';

const { Header} = Layout;
export default class Hea extends Component{
    render(){
        return(
            <Header className="hea">jajaskljaldkHeader</Header>
        )
    }
}